namespace EfCoreExample.Application.Entities
{
    // one order belongs to one customer
    // ?? one order can have multiple positions
    // one order can have multiple products
    public class Order : Entity
    {
    }
}
